```{r, echo=FALSE, include=FALSE}
  ###  This file may be necessary ONLY if you need to copy over assets
  ###  For example, images/plots/charts NOT included in the report using
  ###  the bookdown::include_graphics (which seems to make a copy where needed)

  if(!file.exists(file.path("site", "assets"))) dir.create(file.path("site", "assets"))
  file.copy(file.path(params$base.directory, "assets", "img"), file.path("site", "assets"), recursive = TRUE)
  # if(!file.exists(file.path("site", "assets", "css"))) dir.create(file.path("site", "assets", "css"))
  # file.copy(file.path(params$base.directory, "assets", "css", "literasee_bookdown_style.css"),
  #           file.path(params$working.directory, "site", "assets", "css", "literasee_bookdown_style.css"), recursive = TRUE)
```
